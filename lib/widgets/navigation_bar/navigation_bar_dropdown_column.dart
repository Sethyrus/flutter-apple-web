import 'package:apple/models/navigation_bar/navigation_bar_dropdown_column_config.dart';
import 'package:flutter/widgets.dart';

class NavigationBarDropdownColumn extends StatelessWidget {
  final NavigationBarDropdownColumnConfig config;

  const NavigationBarDropdownColumn({
    super.key,
    required this.config,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
