import 'package:flutter/widgets.dart';

class NavigationBarDropdown extends StatelessWidget {
  const NavigationBarDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
